# Firefly Run — Clean Build
No service worker. Background ignores pointer events. Buttons are real <button> elements.

## Deploy
Upload these files to the repo root (alongside CNAME). Open your site.
